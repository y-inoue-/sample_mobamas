<br><br><br><br><br>
<hr>

<%= link_to 'マイスタジオ', :controller => 'my_studio', :action => 'index' %><p>

<%= link_to '他ユーザーのページ', :controller => 'user_page', :action => 'index' %>
<br>
<p style="text-indent:2em">
<% User.all.each do |user| %>
  <%= link_to user.name, :controller => 'user_page', :action => 'index', :user_id => user.id %>
  <% end %>
<p>

<%= link_to 'ユーザー切り替え', :controller => 'user_switch', :action => 'index' %>
<p style="text-indent:2em">
<% User.all.each do |user| %>
  <%= link_to user.name, :controller => 'user_switch', :action => 'index', :user_id => user.id %>
  <% end %>
<p>

<%= link_to 'modelsのER図', image_path("erd.png") %> 
