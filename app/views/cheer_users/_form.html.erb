<%= form_for(@cheer_user) do |f| %>
  <% if @cheer_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cheer_user.errors.count, "error") %> prohibited this cheer_user from being saved:</h2>

      <ul>
      <% @cheer_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :target_id %><br />
    <%= f.number_field :target_id %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.check_box :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
